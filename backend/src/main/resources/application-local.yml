spring:
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true

proofpay:
  payment:
    active-provider: mock
  notification:
    async: false   # synchrone en local pour faciliter le debug

management:
  endpoint:
    health:
      show-details: always

logging:
  level:
    com.proofpay: DEBUG
